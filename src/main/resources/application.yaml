server:
  port: 8090

spring:
  application:
    name: jangmin

  data:
    redis:
      host: localhost
      port: 6379

  datasource:
    url: jdbc:mysql://localhost:3306/jangmin
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "12345"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: jminhyeog490@gmail.com

    password: qqpcvkoajflykmww  # 띄어쓰기 없는지 다시 확인!
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

    logging:
      level:
        org.springframework: DEBUG
        com.example.jangmin: DEBUG